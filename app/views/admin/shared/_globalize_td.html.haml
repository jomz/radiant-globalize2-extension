%td.globalize
  - if Page === page
    - Globalize2Extension.locales.map do |locale|
      - if page.translated_locales.include?(locale.to_sym)
        %img{:src => "/images/admin/#{locale}.gif", :alt => locale }